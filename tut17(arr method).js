// Array Methods
let num = [1, 2, 3, 34, 4];

// let b = num.toString(); // b is now a string
// console.log(b, typeof b);

// let c = num.join(" and ");
// console.log(c, typeof c);

// let r = num.pop(); // pop returns the popped element. pop/remove the el from end
// console.log(num, r);

// let r = num.push(56); // push returns the new array length. push/add a given el at the end
// console.log(num, r);

// let r = num.shift();
// console.log(r, num); // Removes an element from the start of the array nd returns the shifted el

let r = num.unshift(78);
console.log(r, num); //adds an element from the start of the array nd returns the arr length

// H OMEWORK
// APPLY FOR LOOP TO ITERATE EACH ITEM OF ARR
// for (let i = 0; i < num.length; i++) {
//     let element = num[i];
//     console.log(element);
// };
